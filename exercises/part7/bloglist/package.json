{
  "name": "bloglist-app",
  "version": "1.0.0",
  "description": "Blog bookmarking application",
  "private": true,
  "type": "module",
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "concurrently \"npm run dev --workspace bloglist-server\" \"npm run dev --workspace bloglist-client\"",
    "lint": "npm run lint --workspaces",
    "prettier": "npm run prettier --workspaces",
    "prettier:check": "npm run prettier:check --workspaces",
    "preview": "npm run preview --workspace bloglist-client",
    "start": "npm run build && npm run start --workspace bloglist-server",
    "start:playwright": "concurrently \"npm run start:test --workspace bloglist-server\" \"npm run dev --workspace bloglist-client\"",
    "test": "npm run test --workspace bloglist-client; npm run test --workspace bloglist-server",
    "test:playwright": "npm run test --workspace bloglist-playwright"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.0",
    "concurrently": "^10.0.4",
    "eslint": "^9.25.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^16.0.0",
    "prettier": "^3.9.6",
    "vitest": "^4.1.10"
  },
  "volta": {
    "extends": "../../../volta.config.json"
  }
}
